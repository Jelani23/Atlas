# Memory and tool checkpoint — September 13

## Latest app acceptance and roadmap pause

The user completed the resumed app checks. Both `What all do you remember about me` and `Anything else?` selected all 27 stable user-profile records, with available=27, selected=27 and omitted=0. Replies recovered favorites and additional profile details. The separate favorites follow-up was not included in this transcript, but the first reply already recalled the previously missing favorites; do not request another repeat solely for that omission. The follow-up wording still prematurely suggests completeness and exposes internal record terminology; retrieval coverage passing does not prove faithful/exhaustive natural-language reporting. Totals were 5798 ms and 4028 ms, with larger memory prompts than the small utility fast path.

`Can you explain how to delete a note?` correctly stayed on the LLM conversation route, with no deletion or approval prompt. Its answer nevertheless claimed a delete_note command, API endpoint and note trash icon without supplied implementation evidence. Code inspection confirms a deleteNote backend tool plus delete_note normalization, named-note inputs and approval policy. The UI files searched exposed a conversation trash icon, not a note-specific trash control; the claimed note endpoint/UI guidance was not verified. `Convert five meters to kilograms` correctly reached convertUnit and rejected incompatible dimensions in 812 ms. The pipeline SUCCESS log means the request was handled; it is not evidence that the invalid conversion succeeded.

The user wants a planning break to review accumulated ideas. This is a useful validated checkpoint, not completion of semantic/canonicalization work. The broad comparator remains 49/61, scope/paraphrase weaknesses and arbitrary tool routing remain open, and the proposal-tool workflow mismatch remains unresolved. No additional SQL or repeat of these same app tests is pending. Record next work through roadmap prioritization rather than extending the current patch indefinitely.

## Capability and pipeline awareness requirement

Alice should explain supported operations, the input each needs, speech-compatible ways to request it, approval behavior, limitations and current availability, and understand Atlas's major processing pipelines. Explanations must describe actual supported interfaces and never execute the operation merely because it is being discussed.

There is an existing world_model reader and prompt section. However, contextBuilder's capability gate still describes reliance on semanticProfile classification, while conversationEngine now derives the category from toolResult alone on normal chat. For an explanation with no executed tool, the category defaults to conversation; only a short fallback phrase list can expose the world-model section, and the user's note-deletion wording does not match it. The world model is a stored summary, not an automatically synchronized inventory of executable tools and interfaces.

Proposed next design: a capability catalog assembled from executable tool contracts, permission policies and runtime availability, with checked usage descriptions/examples and distinct workflow entries for multi-step operations. Supplement it with a concise, versioned guide to input/STT, intent/planning, context retrieval, generation/tool results, speech output and background memory ingestion. Derive what can be derived from code; review maintained explanations when the relevant code changes. Retrieve actual code or current runtime evidence for detailed implementation questions. Do not stuff the entire backend into every prompt or treat a stored architecture summary as proof that a feature is deployed. This shared Atlas operating knowledge belongs separately from each agent's personality profile.

Suggested roadmap discussion order: capability/pipeline awareness and restored Alice personality; agent-owned profile design and controlled evolution; contextual conversation/reasoning modes; voice/STT reliability; passive sourced knowledge and topic subscriptions; larger multi-agent/device work. This is a proposal for user discussion, not an approved implementation sequence. Keep remaining memory/tool regressions explicitly scheduled within that roadmap.

## Resumed implementation: profile recall and tool boundaries

The user restored coding authorization after the Luna planning-only turns. This continuation implements profile recall and a bounded tool-routing/correctness pass. Passive knowledge and agent-profile tables remain planned; no migration or production write was performed.

Read-only Supabase inspection confirmed all 28 user_profile rows still exist, including favorite_color, favorite_anime, favorite_food, favorite_drink, favorite_game, favorite_animal, favorite_superhero and favorite_basketball_team. One row is the current-project state pointer, leaving 27 stable profile records. The earlier attached log showed eight selected personal records; the earlier claim that that specific attachment also logged a fresh 28-row load was imprecise. The new database inspection establishes the count independently.

The original diagnosis also missed an important recognition failure: all three supplied phrasings took the conversation budget because the old self-recall detector expected narrow literal substrings. Added profileRecall.js for direct personal-recall questions, favorites and immediately related user follow-ups; assistant prose cannot establish the recall topic. Explicit recall now allows all stable profile rows within a 1600-token estimate rather than a four-plus-four record cap. Current recall terms outrank prior activation, favorites get priority when requested, and normal incidental personalization retains its smaller limit. Supplied/omitted counts and unavailable reads are exposed to prompt assembly and logs; failed profile reads throw instead of being cached as an empty profile. The prompt distinguishes a partial snapshot from absence and requests faithful recall without reciting internal counts.

The isolated real-profile/local-model replay supplied all 27 stable records using about 492 estimated profile tokens on each of the three turns. Alice recalled the omitted favorites instead of claiming none were recorded. Reports are in backend/.local/profile-recall-evaluations (1789328569408.json and 1789328821290.json). These isolate other memory stores and do not exercise live conversation persistence or reflections. The first replay also exposed embellished recall and metadata narration; guidance was tightened, but natural-language faithfulness is still an app acceptance criterion, not a proven accuracy guarantee. The final prompt generalizes the guidance against inferring personality traits from preferences. diagnoseProfileRecall.js reads the real profile once, blocks subsequent non-Ollama fetches and never invokes conversation persistence/extraction or tools.

Mocked planner probes reproduced unwanted deletion approval prompts for both an explanation and an explicit denial. A shared boundary now blocks recognized explanatory, negated and past-discussion forms in intent resolution, normalization, planner routing and semantic clause validation. Confirmation handling is anchored to actual replies instead of any embedded yes; denials take precedence. Corrected the fallback delete-note pending action to carry the executable name and argument array. All mutation probes use mocked execution; no notes were created, renamed or deleted. This remains a conservative fast boundary, not full understanding of arbitrary mixed conversation/action clauses or all confirmation paraphrases.

The converter now rejects mismatched dimensions, malformed numbers, unsupported/prototype-name units and non-finite results. A whole-clause parser shares bounded spoken-number normalization with unit extraction, so standalone requests such as `Convert five kilometers to meters` and `Convert five meters to kilograms` reach the correct tool. Compound requests still go through segmentation. Existing binary storage factors are preserved; temperature/time conversions, broad verbal numbers and currency ambiguity are not completed by this change. The propose_code_change/writeProposal schema and argument-shape mismatch remains open and needs a deliberate workflow fix, not just an export alias.

Next app check: restart Atlas and use a new chat. Ask `What all do you remember about me`, then `Anything else`, then `What about some other of my favorite things`. Expect broader profile recall and favorites; logs should show Profile recall with available/selected/omitted counts (currently 27/27/0 for this profile). Then ask `Explain how to delete a note` (explanation, no approval or action) and `Convert five meters to kilograms` (explicit incompatible-dimensions error, no numerical conversion). Share replies and backend logs. No SQL or invented profile facts are needed. The earlier successful multi-tool wording tests do not need another manual repeat.

Validation after the final code changes: all 62 offline test files pass. All 13 isolated local-model proposal/compilation cases pass again (backend/.local/tool-plan-evaluations/1789329025392.json), including the two spoken multi-tool cases. Tests include complete/partial/failed profile reads, favorites beyond the former cap, scope-changing follow-ups, negative tool requests, semantic proposal rejection, actual confirmations, mixed yes/denial replies, dimension mismatch and standalone spoken unit parsing. These bounded suites do not resolve the older 49/61 memory-comparator limitation or establish general factual accuracy.

## Accepted presentation and next acceptance work

The user has now accepted the conversational tool reports. The restart/repeat requests below are historical and complete. No SQL is pending for these changes.

Next priority is route separation: explanation, negation and ordinary conversation must not trigger a tool just because they contain its vocabulary. Use speech-compatible app probes such as `Explain how to rename a variable in JavaScript`, `What does character count mean`, and `I am not asking you to calculate anything explain what a percentage represents`. Each should receive an explanation without a tool action. Exercise destructive or state-changing negative cases with mocked tools locally before any live test.

Then broaden tool correctness and completeness: invalid unit dimensions, omitted/ambiguous arguments, unsupported actions, partial failures, dependent requests and paraphrases. The known converter dimension gap and proposal export mismatch below remain concrete engineering work. Passing 13 fixed local-model planning cases is not coverage of the full tool catalog.

Memory acceptance should cover a real fact or preference stated by the user, paraphrase deduplication, recall in a new chat and after restart, and separation between personal preferences, Atlas facts and general knowledge. Test corrections with an actual change or an isolated fixture, never a fabricated production fact. Check stored identities/review outcomes as well as Alice's wording. The earlier 49/61 comparator result and reproduced response-grounding failure remain unresolved; the 59 passing offline files do not supersede those findings.

## Profile recall regression observed in app

The user tested a self-memory question, followed by requests for anything else and other favorite things. Alice returned the known music background, timezone, work style, Timberwolves preference and visual-progress preference, but said no other favorites were recorded.

The backend log shows MemoryCache loaded 28 items for user_profile, followed by Hot Cache Updated Personal: 8. This is a retrieval/context-selection limitation, not evidence that the profile table is empty or that the answer came only from chat summaries. For the self-memory path, contextManager.js selects at most four identity/relationship essentials and four dynamic profile entries. The remaining profile rows, including possible favorite color, anime, food or drink records, are excluded before prompt construction. The model therefore cannot mention them, even if they are correctly stored.

This is a priority profile-recall regression. The eventual fix must preserve the context budget while giving explicit self-memory questions a complete or deliberately summarized profile view, and must distinguish not retrieved, not stored, and not found. A read-only profile audit should first confirm which favorite records exist and which eight keys were selected; no production deletion or SQL write is needed. Alice also should not infer that omitted records do not exist.

## Alice identity/personality architecture concern

The user also observed that Alice no longer seems to express the fuller identity/personality defined in atlasState.js. Code inspection shows atlasState.js is still imported by personalityEngine.js and contextBuilder.js, so it has not been disconnected. However, the prompt compiler primarily consumes atlasState.identity and repeats a short hard-coded personality description; the richer traits, values, reasoning principles, preferences, lighter dislikes, aesthetic and avoid lists are not passed through as structured prompt data. The user's impression that the expansion diluted or replaced the original personality is therefore credible, even though the module remains technically wired.

The name atlasState is misleading now that Atlas is the host and Alice is the assistant. Preserve a stable core identity and distinguish it from evolving preferences, ideas and self-observations. Future durable profile changes need ownership, provenance and revision history, with the update policy still to be designed; no agent-profile schema or autonomous self-update policy has been implemented.

This separation should support multiple agents through separate tables or agent-scoped records. Shared user/project/knowledge/procedural memory retains its own ownership and access rules; shared does not imply every agent may read or change everything. Personality data should be assembled by a common prompt compiler rather than hardcoded per agent. An agent preference must not outrank the user's task instructions or substitute for factual evidence. Treat these as distinct kinds of data and authority, not a blanket ranking in which agent personality overrides user context. Agent-owned writes must identify the target agent so one agent cannot accidentally modify another's profile.

## Regular use and passive knowledge roadmap

Begin everyday supervised use now for conversation, brainstorming and low-impact utilities, with implementation-grounded review for coding and important recalled details. This is a staged use recommendation, not a declaration that autonomous actions or factual recall are reliable enough without review. Broader readiness requires route-negative coverage, faithful argument/result handling, cross-session memory round trips, safe correction/scope handling, and honest behavior when tools or evidence fail. Actual use should supply regression examples; completion is determined by these acceptance cases rather than the old informal 80 percent estimate.

The user wants passive world knowledge from trusted news sources, with configurable interests such as NBA, Twitch/streamers and anime. Plan this after the ingestion/retrieval and routing foundation is stable enough to avoid multiplying misclassified or stale memories. Prefer feeds or supported APIs where available; evaluate scraping only where needed. Preserve source URLs, publication/event times, topic relevance and freshness; deduplicate repeated coverage and retain updates/conflicting reports. Collected articles and provisional claims must remain distinct from verified knowledge, rumors, predictions and user/project facts. Source reputation alone must not automatically verify every claim. Offer bounded background collection and optional digests, with topic/source controls and a pause option.

This is a planned feature, not an implemented collector or enabled daily automation. Existing knowledge verification includes source heuristics and temporal checks, but is not a curated news-source registry or complete news trust policy. Preserve casual personality while making sourced recall and uncertainty usable in conversation.

## App success and conversational tool reports

The user confirmed both punctuation-free app requests completed both tools correctly: arithmetic/word count returned 4 and 2 in 1701 ms; conversion/character count returned 5000 meters and 5 characters in 1728 ms. Both took the `multi_tool` fast path with zero context-building time. TTS synthesis started. These logs validate the app's handling of the supplied text; they do not establish microphone transcription quality.

The user requested more natural Alice-style reports instead of `Task 1`/`Task 2` labels. Added `response/toolResultPresenter.js` to the immediate response path. It renders known utility output shapes as conversational sentences, preserves order and all result values, and retains raw tool records for evidence/debugging. Example: `Here you go. 2 + 2 is 4. “hello world” has 2 words.` Conversion output drops only redundant decimal zeros: `Here you go. 5 kilometers comes to 5000 meters. “Atlas” has 5 characters.` Different with/without-space character counts remain explicit. Unknown formats, code and detailed reports stay verbatim.

Partial failures get a partial-completion introduction and retain each error; executor failure detection also recognizes standard `Error reading ...`-style messages. Approval prompts and standalone short-circuit replies are unchanged. Search synthesis continues to use the original evidence path. This presentation adds no model call. It is a small conversational rendering layer, not a new personality-generation model.

All 59 offline test files pass, including presentation tests using actual local utility results, evidence preservation, partial/all failure, exact code preservation and existing routing. The user can restart Atlas and repeat either successful request to review the new wording. No SQL or additional canonicalization fixture test is needed for this presentation change. Memory matching and broader response-grounding limitations below remain open.

## Latest user app result

The memory-bank explanation and dry joke were acceptable. The SQLite answer correctly described normal file persistence, then unnecessarily pivoted to Atlas and falsely called Supabase-backed memories ephemeral/session-bound. Supabase supplies a persistent PostgreSQL database, and Atlas's durable memory tables are not erased on chat closure (https://supabase.com/docs/guides/database/overview). No further response prompt changes were made in this continuation; that grounding/context issue remains open.

Request totals were 3190, 5682 and 2941 ms, all with zero thinking tokens. The first question and joke skipped extraction. The memory explanation unnecessarily triggered extraction because a generic project mention and its registered name together reached score 4; the extractor returned an empty array and no fact was saved. These turns are not tests of equivalence or correction handling, and do not improve the previous 49/61 memory comparator result.

## Implemented

- Project references alone no longer make a turn eligible for memory extraction. An independent memory signal is required. Regression tests cover the exact app explanation, other reference-only messages, real Atlas facts, explicit memory requests and standing instructions. This is a narrow eligibility correction, not a full question/assertion parser.
- Semantic tool proposals require a finite numeric confidence from 0.9 through 1. Previously missing confidence passed the less-than check. Null steps also fail validation cleanly.
- State-changing semantic proposals must match both the independently parsed tool and its arguments. Note filenames normalize whitespace to underscores using the existing execution convention; note content and search queries remain unchanged. Different targets or content still fail. Unresolved `USE_LAST` note targets are rejected on the semantic path.
- Added `src/tools/toolArguments.js`, defining ordered inputs for the known tool catalog and validating count, type, required values, optional inputs, variadic IDs and bounded string/array sizes. The semantic catalog previously supplied null parameter definitions, causing the model to choose correct tools with empty argument arrays. Tools without an explicit contract are excluded from semantic proposals and rejected by validation; coverage tests require contracts for all currently executable registered tools.
- The semantic prompt now explains positional values versus parameter objects. Object-wrapped argument arrays fail validation; they are not silently unwrapped or guessed.
- Semantic fallback now recognizes clauses already identified by the intent resolver even if their first word is absent from trigger prefixes. This fixed the complete request `Convert 5 kilometers to meters; Count the characters in Atlas`, where the first clause lacked a parsed source unit and the second clause's opening word previously prevented recovery.

Deterministic routes, execution permissions, memory verification rules and the model configuration were preserved. Argument contracts apply to semantic plan validation, not every standalone tool call. No migrations, production memory writes, note changes or actual tool operations were performed by the local model evaluator.

## Evidence

All **57 offline test files pass** with network access blocked; the allowlist now includes the three existing tool-planning tests and the new argument-contract test. Tests cover malformed confidence, altered mutation targets, required inputs, numeric types, optional and variadic arguments, note-name normalization, fallback recovery and existing approval preflight. `git diff --check` passes.

`scripts/evaluateToolPlanSemantics.js` calls only local Ollama, blocks database access and replaces tool execute functions with throwing guards. It saves prompts, model options, proposals, selected source snapshots and per-case results. The latest harness also exercises actual segmentation and deterministic-first compilation, reusing the captured proposal if the compiler requests semantic fallback; it never runs the resulting plan.

Reports under `backend/.local/tool-plan-evaluations`:

- `1789246329711.json`: baseline 4/8, with correct tool names but empty required arguments.
- `1789310094482.json`: catalog correction 8/8; this run loaded the compiler before argument validation was added, so it is not final validation evidence.
- `1789310181514.json`: 8/8 with argument validation.
- `1789310313859.json`: expanded 10/11; numeric case returned parameter objects, correctly rejected.
- `1789310377368.json`: positional-value guidance, 11/11 proposal/validation checks.
- `1789310423721.json`: adding actual compilation exposed a fallback-gating failure, 10/11.
- **`1789310497688.json`: final 11/11 including actual compilation**, with correct ordered arguments for arithmetic, word count, conversion, character count, separate search domains and multiword note targets. Incomplete, unsupported and dependent requests did not produce a valid complete plan.

These are small fixed-case checks, not broad statistical accuracy or tool-output correctness benchmarks. The main foreground model and native thinking settings were not changed.

## Next user check and remaining scope

The user clarified that voice will be the main input method. Restart Atlas and test these harmless, speech-compatible requests without punctuation:

1. `Calculate two plus two and then give me the word count of hello world` — both 4 and word count 2 should be returned.
2. `Convert five kilometers to meters and then count the characters in Atlas` — both 5000 meters and character count 5 should be returned.

Follow-up local validation: all 58 offline test files and 13 local model/compilation cases pass (`1789311571353.json`). A bounded arithmetic normalizer now converts common spoken integers/operators to calculator syntax and rejects unsupported wording instead of deleting it. Earlier speech probes (`1789311320045.json`, `1789311361176.json`, `1789311409536.json`) retained a 12/13 failure where arithmetic words were copied unchanged; prompt guidance alone did not resolve it. The normalizer is shared by semantic argument canonicalization and the calculator itself. See `memory-voice-regressions-2026-09-09.md` for supported scope and the broader deferred STT work. No microphone transcription was tested by the local harness.

Ask for replies and backend logs to verify the actual app's routing, execution and delivery of both results. No SQL or fictional project-memory facts are needed. Do not repeat the SQLite conversation test yet; its remaining response issue is already reproduced.

The semantics phase remains open. Memory identity/scope reliability is still 49/61 in the earlier evaluation; existing real SQLite duplicates remain separate, and assertion-mode second-pass comparison remains disabled. Tool argument structure is now stronger, but low-risk argument meaning, broader paraphrases, dependency plans, fallback behavior and tool result correctness still need broader acceptance coverage. Existing code also has a `propose_code_change` schema versus `writeProposal` export-name mismatch, and the unit converter's shared factor table lacks dimensional checks; neither was changed by this continuation. They should be included in subsequent tool correctness work rather than treating the 11-case plan result as proof all tools are correct.
