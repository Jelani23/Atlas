# Boolean return outlines

Opt-in analysis without quoted/scenario inputs now renders a structural Boolean return outline when the selected function is entirely one supported return expression. Shared syntax rules cover negation, strict equality/inequality, Boolean literals, parenthesized grouping, and AND/OR whose operands are independently Boolean. Arbitrary operand-returning AND/OR expressions are excluded. Defaults/destructuring, generators, preceding statements, multiple return paths and loops are excluded from this first subset.

The outline preserves true/false polarity and root short-circuit order. Async functions are described as promise fulfillment. Every rule is conditional on normal evaluation: leaf calls, receivers, getters, coercion and exceptions are unresolved. This is not execution, intrinsic binding resolution, complete semantic verification or a natural-language question entailment validator.

The response uses source citations and authored wording. The supported route makes no model call, so competing model prose cannot reverse its return rule. Logs identify boolean_outline rather than model. Speech uses short expression descriptions while the screen retains exact source expressions. The lexical inventory remains on screen. Unsupported functions and concrete scenarios retain their existing routes; getRecoveryAppend's loop explanation has not been fixed by this change. Follow-ups may receive only the supported return rule, not a complete answer to every possible question.

Function record analyzer version advances to 5 so cached records cannot silently reuse the older structure. No database writes, migration, global defaults, background scan or analyzed/generated code execution was introduced.

Validation: 110/110 offline files passed in backend/.local/boolean-outline-tests.log. Targeted outline and spoken pipeline tests subsequently passed after speech rendering changes. Fixed authored truth cases cover the OR polarity regression; source fixtures cover AND, nested grouping, constants, async returns and unsupported types/control flow. The real-source question test establishes no model call for shouldRecoverResponse and preserves the model path for getRecoveryAppend. This establishes the bounded route, not general model accuracy.

Live check: restart backend and use a new chat. Ask “Analyze the recovery file function should recover response.” Expect one consistent OR rule, otherwise false, and evaluation of the right condition only when the left is false. No contradictory model paragraph or generic missing-evidence list. Log stage should be boolean_outline. Then ask “Analyze the recovery file function get recovery append.” This remains the existing model route; assess it as unverified, not a newly solved loop analysis.
